const ProfileSchema = require('./ProfileSchema')
const VehicleSchema = require('./VehicleSchema')
const RideSchema = require('./RideSchema')

module.exports = {
  ProfileSchema,
  VehicleSchema,
  RideSchema
}